'use strict';

/**
 * status-quotation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::status-quotation.status-quotation');
